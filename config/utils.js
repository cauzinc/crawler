const request = require('request')
const fs = require('fs')
const cheerio = require('cheerio')

export default utils = {
    getHttpBodyByUrl(base, index) {
        const url = `${base}/${index}`
        return new Promise((resolve, reject) => {
            
        })
    }

}

